# FIXME(débito MVP): este arquivo NÃO deveria estar commitado em formato legível.
# Foi gerado de forma incorreta no início do projeto e ficou assim.
# Em produção, é o arquivo encriptado real. Aqui é representação fake para o lab.
#
# Conteúdo descriptografado (FAKE — não usar em produção):
# stripe:
#   api_key: sk_test_FAKE_DO_NOT_USE_4eC39HqLyjWDarjtT1zdp7dc
#   webhook_secret: whsec_FAKE_TestSecret123abc
# database:
#   master_password: lab-only-fake-password
# secret_key_base: 6f3c8a9b2d4e7f1a8c0b5d9e2f4a7c3e1b8d6a4c2e9f1b3d7a5c8e0f2b4d6a8c
#
# Em ambiente real, este arquivo está encriptado e RAILS_MASTER_KEY descriptografa.
# Para o treinamento: este conteúdo é o gancho para a S8 (gestão de segredos).
